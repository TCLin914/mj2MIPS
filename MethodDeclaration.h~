#pragma once
#include "UnaryNode.h"
#include <string>

using namespace std;

class FunctionDef: public UnaryNode
{
public:
	FunctionDef(string, Node*);
	void SetSymbolTable(SymbolTable*, SymbolTable*);
    void Accept(Visitor*);
	bool SemanticCheck();
	bool Initialize();
    Symbol* GetFunctionSymbol();
private:
    Symbol* funcEntry;
	string id;
	static const char MISSING_FUNCTION_ERROR[];
};
