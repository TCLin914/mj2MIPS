#pragma once
#include "UnaryNode.h"

using namespace std;

class ArrayLength : public UnaryNode
{
public:
    ArrayLength(Node*); // (int, Expression) Expression.int
    void SetSymbolTable(SymbolTable*, SymbolTable*);
    void Accept(Visitor*);
    bool SemanticCheck();
    bool Initialize();
    //bool IsAssignable();
    bool IsLocalVariable();    
    Symbol* GetSymbol();
private:
    Symbol* varSymbol;    
};
