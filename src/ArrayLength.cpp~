#include "stdafx.h"
#include "Node.h"
#include "ArrayLength.h"
#include "UnaryNode.h"

ArrayLength::ArrayLength(Node* a) : UnaryNode(a)
{
   
}
void ArrayLength::SetSymbolTable(SymbolTable* gSymTable, SymbolTable* lSymTable)
{
    UnaryNode::SetSymbolTable(gSymTable, lSymTable);
}
void ArrayLength::Accept(Visitor* visitor)
{
    visitor -> Visit(this);
}
bool ArrayLength::SemanticCheck()
{
    bool result = UnaryNode::SemanticCheck();    
} 
bool ArrayLength::Initialize()
{
    type = INTIGER_T;
}
