#pragma once
#include "NullaryNode.h"

using namespace std;

class This : public NullaryNode
{
public:
    This();
    void SetSymbolTable(SymbolTable*, SymbolTable*);
    void Accept(Visitor*);    
    bool Initialize();
};
    
    
