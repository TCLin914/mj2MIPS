#pragma once
#include "NullaryNode.h"

using namespace std;

class ThisStatement : public NullaryNode
{
public:
    ThisStatement();
    //void SetSymbolTable(SymbolTable*, SymbolTable*);
    void Accept(Visitor*);    
    bool Initialize();
};
    
    
